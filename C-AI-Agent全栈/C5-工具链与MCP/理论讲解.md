# C5 — 工具链与 MCP（Model Context Protocol）

> **本章目标**：理解 **MCP 要解决的集成痛点**、**Server/Client/Transport** 分工，以及与 **C4 工具注册** 的对应关系；知道 **stdio 与远程** 的安全差异。  
> **学习建议**：运行 [`demos/01_tool_registry.py`](demos/01_tool_registry.py) 与 [`02_stdio_tool_host.py`](demos/02_stdio_tool_host.py)；文末 §7 与脚本一一对照；生产环境 MCP 以 [modelcontextprotocol.io](https://modelcontextprotocol.io) 官方 SDK 为准。

---

## 目录

1. [Why：工具爆炸与重复集成](#1-why工具爆炸与重复集成)
2. [MCP 解决什么问题](#2-mcp-解决什么问题)
3. [核心概念：Server、Client、Transport](#3-核心概念serverclienttransport)
4. [与「手写 OpenAPI 网关」的对比](#4-与手写-openapi-网关的对比)
5. [安全模型](#5-安全模型)
6. [从 ToolRegistry 到 MCP 的直觉映射](#6-从-toolregistry-到-mcp-的直觉映射)
7. [与 `demos/` 脚本对照](#7-与-demos-脚本对照)
8. [Checkpoint](#8-checkpoint)

---

## 1. Why：工具爆炸与重复集成

每个 Agent 框架各写一套「读文件、查库、调 Slack」适配层，**无法复用、测试分裂**。MCP 把 **能力清单 + 调用约定** 标准化，使 **同一实现** 可被多个宿主消费（IDE、Agent 运行时、CLI）。

---

## 2. MCP 解决什么问题

- **发现**：客户端能 **列举** 某 Server 提供的 tools/resources/prompts。  
- **调用**：按协议 **传参、取回内容**（文本、资源引用等）。  
- **演进**：工具实现可独立版本化，宿主侧只做 **协议兼容**。

---

## 3. 核心概念：Server、Client、Transport

- **MCP Server**：挂载具体能力（读仓库、执行查询…）。  
- **MCP Client（宿主）**：发起 list/call，嵌入在自己的产品里。  
- **Transport**  
  - **stdio**：本地子进程管道，低延迟，适合本机工具。  
  - **HTTP/SSE/WebSocket**：远程服务，需注意 **TLS、认证、超时、多租户**。

---

## 4. 与「手写 OpenAPI 网关」的对比

| | MCP | 自建 HTTP 网关 |
|--|-----|----------------|
| 目标 | 工具/资源 **跨宿主复用** | 服务 **自有业务 API** |
| 形态 | 协议 + 多语言 SDK | REST/GraphQL 自定 |
| 适用 | 桌面/Agent 生态集成 | 对外稳定业务边界 |

二者可并存：**MCP 调本地工具**，**HTTP 调公司订单服务**。

---

## 5. 安全模型

- 任何工具都可能 **读盘/出网** → **最小权限**、路径白名单、出站 allowlist。  
- **远程 MCP** 必须 **鉴权**（Token/mTLS），防 SSRF 与凭证窃取。  
- **日志**：参数脱敏；**多租户** 实例隔离或强制带 tenant 头。

---

## 6. 从 ToolRegistry 到 MCP 的直觉映射

`demos/01_tool_registry.py` 中的 **`list_openai_tools` + `call`**，在概念上对应 MCP 的 **列举工具 + 调用**；`02_stdio_tool_host.py` 演示 **进程边界** 上的 JSON 命令（教学用，非完整 MCP 帧格式）。正式开发请用官方 **`mcp` Python 包** 生成符合规范的 Server。

---

## 7. 与 `demos/` 脚本对照

| 脚本 | 对应理论重点 |
|------|----------------|
| `01_tool_registry.py` | §6 ToolRegistry、与 OpenAI `tools` 列表对齐 |
| `02_stdio_tool_host.py` | §3 Transport（stdio）、list/call 协议直觉 |

详见 [`demos/README.md`](demos/README.md)。

---

## 8. Checkpoint

- [ ] 说清 stdio 与远程 Transport 各适合什么部署。  
- [ ] MCP 与 Function Calling 的分工边界在哪？  
- [ ] 你会把哪类能力第一个做成 MCP Server？

---

**前序**：[C4 — Function Calling](../C4-Function-Calling与结构化输出/理论讲解.md)　**后续**：[C6 — 单 Agent 开发](../C6-单Agent开发/理论讲解.md)
