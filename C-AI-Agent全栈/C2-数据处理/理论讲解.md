# C2 — 数据处理（面向 LLM / RAG）

> **本章目标**：在文档进入 **嵌入与向量库**、或对话进入 **微调 / 评测集** 之前，掌握**清洗、规范化、结构化与元数据**；能说明「脏数据如何放大幻觉与检索噪声」。  
> **写作原则**：Why → What → How；与 Pandas 官方文档互补，本章只强调 **Agent / RAG 流水线**上的决策点。  
> **学习建议**：按 [`demos/README.md`](demos/README.md) 顺序运行 `01`–`04`（无第三方依赖）；文末 §13 与脚本一一对照；再挑一份真实 Markdown/HTML 自己写清洗函数。

---

## 目录

1. [为什么 Agent 链路里必须有「数据处理」](#1-为什么-agent-链路里必须有数据处理)
2. [常见输入形态与风险](#2-常见输入形态与风险)
3. [字符、编码与规范化](#3-字符编码与规范化)
4. [Markdown 与技术文档](#4-markdown-与技术文档)
5. [HTML / PDF 要点（不展开具体库）](#5-html--pdf-要点不展开具体库)
6. [JSON / JSONL：对话与评测数据](#6-json--jsonl对话与评测数据)
7. [表格数据（CSV / Parquet）](#7-表格数据csv--parquet)
8. [去重、抽样与质量门禁](#8-去重抽样与质量门禁)
9. [PII 与脱敏](#9-pii-与脱敏)
10. [与 C3 分块、元数据的衔接](#10-与-c3-分块元数据的衔接)
11. [工具栈速览](#11-工具栈速览)
12. [Checkpoint](#12-checkpoint)
13. [与 `demos/` 脚本对照](#13-与-demos-脚本对照)

---

## 1. 为什么 Agent 链路里必须有「数据处理」

- **RAG**：页眉、导航栏、乱码表格会进入 chunk → **向量空间被噪声占据** → Top-K 常检索到无意义片段。  
- **Tool 返回**：数据库/API 的嵌套 JSON 若原样塞进上下文，**Token 暴涨**且模型易抓错字段。  
- **微调 / 评测**：JSONL schema 不一致会导致训练脚本静默失败或指标不可比。  
- **可观测性（C8）**：统一字段才能做聚合与告警。

**一句话**：模型「读」的是字节序列；**质量在进模型之前就已决定**。

---

## 2. 常见输入形态与风险

| 形态 | 典型来源 | 主要风险 |
|------|----------|----------|
| Markdown | 文档站、笔记 | front matter、重复标题、内嵌 HTML |
| HTML | 爬虫 | 脚本/style、模板重复块 |
| PDF | 论文、合同 | 换行错乱、多栏顺序错 |
| JSON/JSONL | 日志、导出对话 | 字段缺失、一行超大 JSON |
| CSV/Parquet | 业务表 | 缺失值、类型推断错、泄漏敏感列 |

---

## 3. 字符、编码与规范化

- **统一 UTF-8**：读文件显式 `encoding="utf-8"`，对非法字节用 **`errors="replace"`** 并计数告警。  
- **Unicode 规范化**：`unicodedata.normalize("NFKC")` 处理全角/兼容字符（注意：可能改变语义，需业务确认）。  
- **零宽字符与 BOM**：影响字符串匹配与去重，应剔除（见 `demos/03_normalize_text.py`）。  
- **空白**：行尾空格、连续空行影响分块边界与展示，可压缩为「最多双换行」。

---

## 4. Markdown 与技术文档

- **YAML front matter**：元数据有用，但**嵌入前常需剥离**，避免进入向量正文。  
- **代码块**：决定是否与正文一起嵌入；有时应对 **代码块单独建 chunk** 并打 `language` 标签。  
- **链接与图片**：纯文本流水线可保留 `[text](url)` 或替换为 `text（见 url）`，避免向量里只有 URL。  
- **演示**：`demos/01_clean_markdown.py`。

---

## 5. HTML / PDF 要点（不展开具体库）

- **HTML**：优先抽 **main/article** 区域；剔除 `script/style/nav`；表格考虑转 Markdown 或单独存储。  
- **PDF**：先选成熟解析库；对扫描件需 **OCR** 流水线（另一岗位域）。  
- **原则**：解析器版本升级会导致文本微差，**索引版本**要与解析版本绑定记录。

---

## 6. JSON / JSONL：对话与评测数据

- **一行一条**（JSONL）适合大文件流式读。  
- **role 白名单**：`system` / `user` / `assistant` / `tool`（若用工具）。  
- **每条必有稳定主键**（如 `id`、`session_id`）便于去重与回归。  
- **演示**：`demos/02_jsonl_messages.py`。

---

## 7. 表格数据（CSV / Parquet）

- **Pandas**：`dtype` 显式指定，避免把 ID 读成 float。  
- **缺失值**：`fillna` 策略要可解释；分类任务注意 **类不平衡**。  
- **进 LLM**：常将一行 **渲染为键值文本** 或 **模板句子**，而不是裸 CSV 贴进 prompt（除非任务需要）。

---

## 8. 去重、抽样与质量门禁

- **精确去重**：hash 整段文本。  
- **近似去重**：SimHash/MinHash（大语料常用）；RAG 小规模可用 **embedding 聚类** 粗判。  
- **质量门禁**：最短长度、最大长度、非法字符比例、语言检测（可选）——不通过则 **隔离到 quarantine** 而非静默丢弃。

---

## 9. PII 与脱敏

- **正则 + 规则**可覆盖手机、邮箱等常见模式（见 `demos/04_pii_redact.py`）。  
- **NER 模型**提高召回，有成本。  
- **原则**：脱敏在 **进向量库与写日志之前**；密钥类 **永不下发给浏览器**。

---

## 10. 与 C3 分块、元数据的衔接

- 分块前尽量 **按语义边界**（标题、段落）而非纯 `str[i:i+512]`。  
- 每个 chunk 建议带：**`source_id`、`path`、`heading`、`updated_at`、可选 `acl_tags`**。  
- **ACL**：检索时必须 **先过滤元数据** 再算相似度，避免跨租户泄漏。  
- 详见 [C3 — RAG 与知识库](../C3-RAG与知识库/理论讲解.md)。

---

## 11. 工具栈速览

| 需求 | 常见选择 |
|------|----------|
| 表格 | `pandas` / `polars` |
| HTML | `beautifulsoup4` / `lxml` |
| PDF | `pypdf` / 商用解析 |
| 大流水线 | `unstructured`（重） |
| 校验 | `pydantic` / `jsonschema` |

**原则**：能标准库则标准库；引入重型依赖前确认 **部署镜像与许可证**。

---

## 12. Checkpoint

- [ ] 举出 3 种「脏数据」对 RAG 的具体危害。  
- [ ] JSONL 与单个巨大 JSON 数组各适合什么场景？  
- [ ] 为什么 chunk 需要 metadata 而不仅是纯文本？

---

## 13. 与 `demos/` 脚本对照

| 脚本 | 对应理论重点 |
|------|----------------|
| `01_clean_markdown.py` | §4 Markdown、§3 规范化 |
| `02_jsonl_messages.py` | §6 JSONL、对话结构 |
| `03_normalize_text.py` | §3 字符与规范化 |
| `04_pii_redact.py` | §9 PII 与脱敏 |

详见 [`demos/README.md`](demos/README.md)。

---

**下一站**：[C3 — RAG 与知识库](../C3-RAG与知识库/理论讲解.md)。
