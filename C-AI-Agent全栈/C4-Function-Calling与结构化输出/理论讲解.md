# C4 — Function Calling 与结构化输出

> **本章目标**：掌握 **OpenAI 风格 tools** 的请求/响应形态、**服务端执行**边界、以及 **Pydantic/JSON Schema** 校验；能画出「模型提议 → 宿主执行 → tool 消息写回」的时序图。  
> **写作原则（与 [A1 — Pythonic 与标准库](../../A-Python基础/A1-Pythonic与标准库/理论讲解.md) 对齐）**：先讲清 **Why 痛苦**，再给出 **What 契约**，最后用 **How 代码路径** 对齐 demos；每个大节前 **本节概念**，大节后 **本节小结（三张卡片）**。  
> **学习建议**：先跑 [`demos/01_tool_loop_mock.py`](demos/01_tool_loop_mock.py)（无依赖），再配置 Ollama 跑 [`02_openai_tools_ollama.py`](demos/02_openai_tools_ollama.py)；结构化输出见 [`03_structured_json_pydantic.py`](demos/03_structured_json_pydantic.py)（`pip install -r requirements.txt`）。

---

## 目录

1. [Why：模型不仅要说话，还要触发动作](#1-why模型不仅要说话还要触发动作)
2. [OpenAI Chat Completions 里的 tools 长什么样](#2-openai-chat-completions-里的-tools-长什么样)
3. [一轮完整工具调用时序](#3-一轮完整工具调用时序)
4. [工具 Schema 怎么写才好用](#4-工具-schema-怎么写才好用)
5. [服务端执行与安全](#5-服务端执行与安全)
6. [结构化输出：JSON Mode 与 Pydantic](#6-结构化输出json-mode-与-pydantic)
7. [常见坑与排错](#7-常见坑与排错)
8. [与 C5、C6 的关系](#8-与-c5c6-的关系)
9. [与 `demos/` 脚本对照](#9-与-demos-脚本对照)
10. [Checkpoint](#10-checkpoint)

---

## 0. 与 A1、C1 的衔接

**本节概念**：**Function Calling** 不是「远程执行 Python」，而是 **模型输出结构化意图**（函数名 + 参数 JSON），由 **宿主进程** 像调用普通函数一样执行；这与 A1 里强调的 **「显式优于隐式」** 完全一致 —— 执行路径必须在代码里可见。

你在 C1 已经熟悉 **messages 列表** 与 **流式响应**；C4 在此基础上增加 **`tool_calls` 与 `role: tool` 消息**，列表变长、顺序变严格。用 A1 的思维：**把一轮 tool 往返当成一个小状态机**，比把 API 当「魔法黑盒」好调试得多。

**本节小结（三张卡片）**

- **一句话定义**：Function Calling = **模型提议，宿主裁决与执行**。
- **典型场景**：订单查询只读 API；模型再聪明也不能直连生产库 connection string。
- **和 A1 类比**：`tool_calls` 像 **`getattr(registry, name)(**kwargs)`** —— 名字必须落在白名单映射上。

---

## 1. Why：模型不仅要说话，还要触发动作

**本节概念**：自然语言 **不可被机器安全执行**；业务需要 **查订单、调内部 API、写数据库、触发工作流**，必须落到 **确定性的函数名与参数**。

如果让模型直接输出「帮我执行 SQL」，你得到的是 **字符串级别的 RCE 风险**。Industry 做法是把能力 **收敛为有限个函数**，每个函数背后是你写的 **类型校验 + 权限检查 + 审计日志**。模型只负责在候选集中 **挑函数与填参数**，不负责 **打开新能力面**。

**本节小结（三张卡片）**

- **一句话定义**：工具调用的本质是 **受限指令集** 上的规划，不是通用脚本引擎。
- **典型场景**：客服机器人只能 `get_order_status`，不能 `drop_table`。
- **和 A1 类比**：像只暴露 **少量公开方法** 的类，把危险操作藏在 **私有方法** 里，外部永远调不到。

---

## 2. OpenAI Chat Completions 里的 tools 长什么样

**本节概念**：每条 tool 在协议层是 **JSON Schema 子集** + **自然语言描述**；描述会进上下文，影响模型是否选对工具。

每条 tool 通常为：

```json
{
  "type": "function",
  "function": {
    "name": "get_invoice",
    "description": "按订单号查询电子发票链接（只读）",
    "parameters": {
      "type": "object",
      "properties": {
        "order_id": { "type": "string", "description": "订单号" }
      },
      "required": ["order_id"]
    }
  }
}
```

请求体里带 `tools: [...]`；`tool_choice` 常用 `"auto"`（由模型决定是否调用），也可设为 `"required"` 或指定某一 `function.name` 以强制走工具。

**实践提示**：`description` 与 `properties[].description` 是 **给模型读的文档**；写得太短，模型会乱选；写得像内部黑话，模型会胡填。保持 **用户视角 + 边界条件**（只读/写入、失败时返回什么）。

**本节小结（三张卡片）**

- **一句话定义**：tools 列表是 **宿主提前注册的「可调 API 目录」**。
- **典型场景**：同一 `name` 在开发/生产指向不同 base URL，由宿主映射，不进 schema。
- **和 A1 类比**：parameters schema 像 **TypedDict / dataclass 的字段约束** —— 先约束形状，再谈业务逻辑。

---

## 3. 一轮完整工具调用时序

**本节概念**：一轮往返里，**assistant 消息可能带 `tool_calls`**；宿主执行后必须用 **`role: tool`** 把 **observation** 写回，且 **`tool_call_id` 对齐**，模型才能把「行动结果」纳入后续推理。

1. Client → API：`messages` + `tools`。  
2. API → Client：`assistant` 消息带 **`tool_calls`**（含 `id`、`function.name`、`function.arguments` 字符串）。  
3. Client：**校验 name 在白名单**，`json.loads(arguments)`，**执行业务函数**。  
4. Client → API：追加 `assistant` 原消息，再追加若干 **`role: tool`**，`tool_call_id` 对齐，`content` 为字符串结果。  
5. API → Client：模型基于观察生成 **最终自然语言**（或再次 tool_calls）。

**执行权永远在服务端**；模型不能直连生产网。**观察**尽量是 **机器生成的客观字符串**（查询结果、错误码），减少模型编造空间 —— 这一点在 C6 ReAct 里会反复出现。

**本节小结（三张卡片）**

- **一句话定义**：时序本质是 **「提议 → 执行 → 观察写回」** 的闭环。
- **典型场景**：多工具并行调用时，多个 `tool_call_id` 与多条 `tool` 消息一一对应。
- **和 A1 类比**：像 **递归前先压栈** —— 不把 `assistant` 原消息带着，`tool` 就成了无根之木。

---

## 4. 工具 Schema 怎么写才好用

**本节概念**：好 schema **减少歧义**、**减少非法参数**、**减少上下文体积**；坏 schema 让模型在几十个工具里 **瞎蒙**。

- **`description`**：写清 **只读/写入**、**副作用**、**失败时返回什么**。  
- **`required`**：宁多勿少；可选参数给 `default` 或在描述里说明默认行为。  
- **`enum`**：限制取值，减少胡编。  
- **体量**：工具过多过长会挤占上下文，需 **动态筛选** 或 **分层工具**（按会话阶段只挂载子集）。

**本节小结（三张卡片）**

- **一句话定义**：Schema 是 **人机共读契约**，偏向模型可读性设计。
- **典型场景**：日期字段要求 `YYYY-MM-DD`，用 pattern 或描述写死，避免 `2025/4/15` 与 `15-04-2025` 混用。
- **和 A1 类比**：`enum` 像 **Literal 类型** —— 把「魔法字符串」收窄成安全集合。

---

## 5. 服务端执行与安全

**本节概念**：**任何** `function.name` 最终都会映射到宿主代码；宿主必须假设 **模型是恶意的合谋者** —— 会尝试越权参数、SQL 片段、路径穿越。

- **白名单**：仅允许注册过的 `name`。  
- **参数校验**：用 JSON Schema 或 Pydantic **二次校验**（防注入、类型漂移）。  
- **权限**：结合登录态 / API Key / 租户 ID 决定能否调用某工具。  
- **限流与审计**：每次调用记 **谁、何时、参数摘要、耗时、错误**。

**本节小结（三张卡片）**

- **一句话定义**：安全模型 = **身份 + 授权 + 审计 + 限流** 四层，缺一不可。
- **典型场景**：工具参数里带 `../../etc/passwd`，白名单挡不住路径穿越，要在函数内再校验 realpath。
- **和 A1 类比**：二次校验像 **`@wraps` 外层再包一层权限装饰器** —— 业务函数专注逻辑，横切关注点统一处理。

---

## 6. 结构化输出：JSON Mode 与 Pydantic

**本节概念**：**JSON Mode** 保证语法是对象；**Pydantic** 保证语义与类型；**Function Calling** 解决「动作」而不仅是「数据形状」。

| 手段 | 适用 |
|------|------|
| **response_format: json_object** | 强制 JSON 语法（字段仍需你在 prompt 里约定） |
| **Pydantic `model_validate_json`** | 校验 + 类型 + 默认值；失败可 **重试或降级** |
| **Function Calling** | 「动作」而不仅是「数据形状」 |

生产上：**parse 失败 → 收紧 prompt 或降温度 → 有限次重试**。不要把「解析失败」静默吞掉，否则线上指标会假阳性。

**本节小结（三张卡片）**

- **一句话定义**：JSON Mode 管括号，Pydantic 管字段含义。
- **典型场景**：抽取发票字段，`amount` 有时是字符串 `"1,234.56"`，Pydantic 前置规范化。
- **和 A1 类比**：`model_validate_json` 像 **`ast.literal_eval` + 类型检查** 的工业加强版。

---

## 7. 常见坑与排错

**本节概念**：排错时区分 **「模型没提议」**、**「提议了但 JSON 坏了」**、**「执行失败」**、**「写回格式错了」** 四层，每层对策不同。

- **模型不调工具**：system 指令不够硬、或模型/服务端 **不支持 tools**；或 **描述含糊** 导致模型选择纯文本回答。  
- **`arguments` 非合法 JSON**：需捕获异常并反馈给模型或走降级；日志里打 **截断后的 arguments**，不打全量敏感参数。  
- **死循环**：同参重复调用 → **max_iterations** + **去重键**（name+args hash）。  
- **把密钥写进 tool 示例**：schema 与 prompt 中一律用占位符。

**本节小结（三张卡片）**

- **一句话定义**：工具排错 = **契约层 + 执行层 + 消息层** 分层二分。
- **典型场景**：Ollama 某旧版对 `tool_calls` 支持不完整，表现为永远无 `tool_calls` 字段 —— 先查服务端版本矩阵。
- **和 A1 类比**：像调试 **多层装饰器** —— 先确定哪一层把异常吃了。

---

## 8. 与 C5、C6 的关系

**本节概念**：C4 定义 **单进程内的工具循环**；C5 把工具 **跨进程/跨产品复用**；C6 把循环 **产品化**（停止条件、记忆、观测）。

- **C5**：把工具 **以标准协议（如 MCP）** 暴露，宿主仍负责鉴权与执行映射。  
- **C6**：ReAct 循环 = 多轮「可能含 tool_calls 的 assistant」与 **tool** 消息交替，直到终止条件。

---

## 9. 与 `demos/` 脚本对照

| 脚本 | 对应理论重点 |
|------|----------------|
| `01_tool_loop_mock.py` | 第 3 节时序、第 5 节安全执行（`safe_calc`） |
| `02_openai_tools_ollama.py` | 第 2 节 tools 形态、第 3 节完整一轮 |
| `03_structured_json_pydantic.py` | 第 6 节 Pydantic 校验与失败路径 |

依赖与命令见 [`demos/README.md`](demos/README.md)（`pip install -r requirements.txt`）。

---

## 10. Checkpoint

- [ ] 默画时序图：从用户提问到 tool 结果写回。  
- [ ] 为什么 `tool_call_id` 必须原样带回？  
- [ ] 结构化输出校验失败时你的三步处理是什么？

---

**前序**：[C3 — RAG](../C3-RAG与知识库/理论讲解.md)　**后续**：[C5 — 工具链与 MCP](../C5-工具链与MCP/理论讲解.md)
